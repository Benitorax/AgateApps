mysql:
    type: mysql:10.3
    disk: 2048
    configuration:
        schemas:
            - agate
            - legacy
        endpoints:
            main:
                default_schema: main
                privileges:
                    main: admin
            legacy:
                default_schema: legacy
                privileges:
                    legacy: admin
